#include <iostream>

    using std::cout;
    using std::cin;
    using std::endl;

int main()
{

    int x, y;
    int sum;
                                            // variables are declared as y and x but an be anything made up by a human
    cout <<"Type a number: ";  
    cin >> x;
    cout <<"Type another number:";
    cin >> y;
    sum = x+y;
    cout << "Total = " << sum << endl;

return 0;

} 